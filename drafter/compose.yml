services:
    drafter:
        build:
            dockerfile: ./Dockerfile
            context: .
        restart: always
        env_file: '.env'

networks:
    default:
        external: true
        name: proxy
